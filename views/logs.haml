%h2 アクセスログ

%table.table.table-striped
  %tbody
    %tr
      %th 日時
      %th ロケール
      %th プロキシ
      %th AssociateTag
      %th 応答時間 (msec)
    -for log in @logs
      %tr
        %td= log.created_at.new_offset(Rational(9, 24))
        %td= log.locale
        %td= log.proxy.name if log.proxy
        %td= log.atag
        %td{style: 'text-align: right'}= sprintf("%d", log.response * 1000) if log.response
